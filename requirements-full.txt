# Additionally required to retrain the heads, re-extract features, or rebuild a
# trained model from weights_heads/ (src/load_trained_model.py).
-r requirements.txt
torch>=2.0
torchvision>=0.15
timm>=0.9
pillow>=10.0
